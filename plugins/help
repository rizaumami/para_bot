help() {

  local COMMAND="${MESSAGE:5}"
  local COMMAND="${COMMAND//\//}"

  if [[ ${MESSAGE:4} = *[!\ ]* &&  -f ./plugins/"${COMMAND}" ]]; then
    local PLUGS="$(< ./plugins/${COMMAND})"
    local CMD_HELP="${PLUGS##*msg=\'}"
    local CMD_HELP="${CMD_HELP%%\'*}"

    msg="${CMD_HELP}"
  else
    msg='Available commands:
    - /about
    - /calc <expression>
    - /cuaca <location>
    - /ddg <query>
    - /echo <text>
    - /help [command]
    - /info
    - /jam <location>
    - /kamus <term>
    - /shalat <location>'
  fi

  send_message

}
