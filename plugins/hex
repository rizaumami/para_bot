hex() {

  if [[ ${MESSAGE:3} = *[!\ ]* ]]; then
    local HEXDEC="${MESSAGE:4}"
    if [[ ${HEXDEC:0:2} = '0x' ]]; then
      msg="$(printf '%d\n' "$HEXDEC")"
    else
      msg="$(printf '0x%x\n' "$HEXDEC")"
    fi
  else
    msg='/hex <number>
    This function converts a number to or from hexadecimal.'
  fi

  send_message

}
