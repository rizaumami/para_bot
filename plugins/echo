echo() {

  if [[ ${MESSAGE%% *} = "echo" ]] ; then
    if [[ ${MESSAGE:4} = *[!\ ]* ]]; then
      msg="${MESSAGE:5}"
    else
      msg='/echo <text>
      Repeat a string.'
    fi

    send_message

  fi

}
